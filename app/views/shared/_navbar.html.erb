<nav class="navbar navbar-expand-lg navbar-dark pb-20px">
  <div class="container">
    <a class="navbar-brand" href="#">
      <%= image_tag "Mano.jpg", size: "100x100" %>
      <%= link_to 'Social Network', root_path, class: 'nav-link text-light'%></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <%= link_to 'Explorar', all_tweets_path, class: 'nav-link text-light'%>
        </li>
        <% if user_signed_in? %>
          <% if current_user.admin %>
            <li class="nav-item">
              <%= link_to 'Panel Administrador', admin_root_path, method: :get,  class: 'nav-link text-light'%>
            </li>
            <% end %>
          <li class="nav-item">
            <a class="nav-link">@<%= current_user.username%></a>
          </li>
          <li class="nav-item">
            <%= link_to 'Cerrar sesión', destroy_user_session_path, method: :get,  class: 'nav-link text-light'%>
          </li>
          <% else %>
        <li class="nav-item">
          <%= link_to 'Registro', new_user_registration_path, class: 'nav-link text-light'%>
        </li>
        <li class="nav-item">
          <%= link_to 'Login', new_user_session_path, class: 'nav-link text-light'%>
        </li>
          <% end %>
      </ul>
    </div>
  </div>
</nav>